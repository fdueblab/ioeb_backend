# 数据库结构说明

## 核心表结构

### 1. services（微服务/成果表）

**表名**：`services`

**主要字段**：
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | VARCHAR(36) | 主键，服务ID |
| name | VARCHAR(100) | 服务名称 |
| attribute | VARCHAR(50) | 服务属性 |
| type | VARCHAR(50) | 服务类型 |
| domain | VARCHAR(50) | 领域 |
| industry | VARCHAR(50) | 行业 |
| scenario | VARCHAR(50) | 场景 |
| technology | VARCHAR(50) | 技术 |
| network | VARCHAR(50) | 网络类型 |
| port | VARCHAR(500) | 端口映射 |
| volume | VARCHAR(500) | 数据卷映射 |
| status | VARCHAR(50) | 服务状态 |
| number | INT | 服务调用次数 |
| deleted | INT | 是否删除：0-未删除，1-已删除 |
| create_time | BIGINT | 创建时间戳（毫秒） |
| creator_id | VARCHAR(36) | 创建者ID |
| **is_for_sale** | BOOLEAN | 是否对外销售 |
| **sale_price** | FLOAT | 销售价格 |
| **sale_description** | TEXT | 销售说明 |
| **sale_status** | VARCHAR(20) | 销售状态：unpublished/published |

**新增字段说明**：
- `is_for_sale`, `sale_price`, `sale_description`, `sale_status`：用于成果销售功能

### 2. user_service_relations（用户成果关系表）

**表名**：`user_service_relations`

**主要字段**：
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INT | 主键，自增 |
| user_id | VARCHAR(36) | 用户ID |
| service_id | VARCHAR(36) | 成果ID |
| relation_type | VARCHAR(20) | 关系类型：developed/purchased/interested |
| purchase_time | BIGINT | 购买时间戳（仅对已购买成果） |
| purchase_price | FLOAT | 购买价格（仅对已购买成果） |
| create_time | BIGINT | 创建时间戳 |

**关系类型说明**：
- `developed`：已开发成果
- `purchased`：已购买成果
- `interested`：感兴趣成果

### 3. service_messages（成果消息表）

**表名**：`service_messages`

**主要字段**：
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INT | 主键，自增 |
| service_id | VARCHAR(36) | 成果ID |
| sender_id | VARCHAR(36) | 发送者ID |
| receiver_id | VARCHAR(36) | 接收者ID |
| message_type | VARCHAR(20) | 消息类型 |
| content | TEXT | 消息内容 |
| is_read | BOOLEAN | 是否已读 |
| create_time | BIGINT | 创建时间戳 |

**消息类型说明**：
- `contact_purchase`：购买联系
- `use_service`：使用服务
- `general`：普通消息

### 4. service_upgrade_advices（成果升级建议表）

**表名**：`service_upgrade_advices`

**主要字段**：
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | VARCHAR(36) | 主键，记录ID |
| service_id | VARCHAR(36) | 关联服务ID（唯一） |
| leading_analysis | TEXT | 领先情况分析 |
| auto_upgrade_suggestion | TEXT | 自动升级建议 |
| manual_update_suggestion | TEXT | 人工更新建议 |
| generated_at | BIGINT | 生成时间戳 |
| generator_user_id | VARCHAR(36) | 触发生成的用户ID |

### 5. service_update_strategies（成果更新策略表）

**表名**：`service_update_strategies`

**主要字段**：
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | VARCHAR(36) | 主键，策略ID |
| service_id | VARCHAR(36) | 成果ID（外键） |
| auto_test_enabled | BOOLEAN | 是否启用自动测试 |
| auto_test_period | INT | 自动测试周期（天） |
| update_strategy_type | VARCHAR(50) | 更新策略类型：manual/auto/scheduled |
| update_config | TEXT | 更新策略详细配置（JSON格式） |
| next_test_time | INT | 下次测试时间戳 |
| last_test_time | INT | 上次测试时间戳 |
| create_time | INT | 创建时间戳 |
| update_time | INT | 更新时间戳 |
| status | INT | 状态：1-正常，0-禁用 |

### 6. users（用户表）

**表名**：`users`

**主要字段**：
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | VARCHAR(36) | 主键，UUID |
| username | VARCHAR(100) | 用户登录名 |
| name | VARCHAR(100) | 用户姓名 |
| password | VARCHAR(255) | 加密后的密码 |
| avatar | VARCHAR(255) | 头像路径 |
| telephone | VARCHAR(20) | 电话号码 |
| merchant_code | VARCHAR(50) | 商户代码 |
| role_id | VARCHAR(36) | 角色ID |
| wx_openid | VARCHAR(128) | 微信小程序openid |
| status | INT | 用户状态：1-正常，0-禁用 |
| deleted | INT | 是否删除：0-未删除，1-已删除 |
| last_login_ip | VARCHAR(50) | 最后登录IP |
| last_login_time | BIGINT | 最后登录时间戳 |
| create_time | BIGINT | 创建时间戳 |
| creator_id | VARCHAR(36) | 创建者ID |

## 其他辅助表

### 7. service_norms（服务规范评分表）
- 关联 services 表
- 存储技术评测结果

### 8. service_sources（服务溯源表）
- 关联 services 表
- 存储知识产权、应用案例等信息

### 9. service_apis（服务API表）
- 关联 services 表
- 存储REST API或MCP工具定义

### 10. service_api_parameters（API参数表）
- 关联 service_apis 表
- 存储API参数定义

### 11. service_api_tools（MCP工具表）
- 关联 service_apis 表
- 存储MCP工具列表

### 12. meta_app_configs（元应用配置表）
- 关联 services 表
- 存储元应用专用配置

### 13. dictionary（字典表）
- 存储系统字典数据
- 如：服务类型、状态等

### 14. roles（角色表）
- 存储用户角色信息

### 15. permissions（权限表）
- 存储权限定义
